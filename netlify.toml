# Used only if Netlify "Package directory" is empty (repo root).
# Current Netlify UI has Package directory = structura-v2/ → see structura-v2/netlify.toml

[build]
  publish = "structura-v2"
  command = "echo 'Structura static publish'"
